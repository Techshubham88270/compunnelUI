import React from 'react'
// import { useSelector } from "react-redux";

function Header() {
//   const state = useSelector((state) => state.cartData.numOfItems
//   )

  return (
    <div>
  test
    </div>
 
  )
}

export default Header